extends _bootstrap
append styles
    // Custom styles for this template
    link(href='/css/navbar-static-top.css', rel='stylesheet')

block body
    // Static navbar
    +navbar-static("Gyungbuk SoS lab","navbar")
        +nav_item("/","active") Home
        +nav_item_dropdown("#")(label="Dropdown")
            +nav_item("/admin/regions") Admin

    .container
        //Iteration for listing village item
        for item in villageList
            .jumbotron
                // Village name
                h3= item.name
                // Placeholder for village description
                p= item.summary_text
                a.btn.btn-lg.btn-primary(href='/regions/' + item._id, role='button') Detail »
                
